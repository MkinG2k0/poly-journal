export const extraReducers = {
    // [Function.pending.type]: (state, action) => setStatus(state.rootStore, action, 'pending'),
    // [Function.rejected.type]: (state, action) => setStatus(state.rootStore, action, 'rejected'),
    // [Function.fulfilled.type]: (state: IRootState, action: PayloadAction<IDataResponse>) => {
    //     setStatus(state.dataOrg, action, 'fulfilled');
    // },
}